<%= form_for(current_user.wishlists.find_by(item_id: @item.id),
             html: { method: :delete },remote: true) do |f| %>
  <%= f.submit "Remove from Wishlist", class: "btn btn-large" %>
<% end %>